'use client'
const EmailTemplateForm = () => {
    return (
        <button onClick={() => document.getElementById('my_modal_7').showModal()} className='bg-green-500 px-10 text-lg btn btn-primary  text-white'>Make an email templates</button>
    );
};

export default EmailTemplateForm;